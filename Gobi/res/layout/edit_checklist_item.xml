<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".EditChecklistItemActivity" >

    <!-- ########################################### -->
    <!-- # EDIT CHECKLIST TEXT FIELD & DONE BUTTON # -->
    <!-- ########################################### -->

    <LinearLayout
        android:id="@+id/task_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="15dp">

        <EditText
            android:id="@+id/editChecklistItemText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ems="10"
            android:inputType="text"
            android:maxLength="140"
            android:hint="@+string/checklist_item_hint" />

        <ImageButton
            android:id="@+id/confirmEditChecklistItemButton"
            android:layout_width="45dp"
            android:layout_height="36dp"
            android:background="@null"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_edit_done" />
    </LinearLayout>
   </LinearLayout>