<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"


    tools:context=".MainActivity" >

    <!-- ##############  TOP MENU  ################# -->

   <!--  <LinearLayout
        android:id="@+id/top_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/workspaceIcon"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_personal" />

        <TextView
            android:id="@+id/workspaceTitle"
            android:layout_width="0dip"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_horizontal"
            android:scaleType="centerCrop"
            android:text="@string/workspace_title"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:textSize="30sp" />

        To set background transparent: android:background="@null"

        <ImageButton
            android:id="@+id/addTaskButton"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@null"
            android:padding="3dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_add_task_normal" />
    </LinearLayout>
 -->
    <!-- ##############  PROJECT LISTVIEW  ############### -->
    
   <!--  <fragment android:id="@+id/tabsFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        class="com.example.gobi.TabsFragment"
        /> -->

    <ListView
        android:id="@+id/ListViewOrganiseProjectList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </ListView>

    <TextView 
       android:id="@+id/noProjectsOrganise"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:orientation="vertical"
       android:visibility="invisible"
       android:text="No projects have been created. Try adding a project first" ></TextView>	
    
</LinearLayout>